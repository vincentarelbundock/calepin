[project]
name = "calepin-docs"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "jupyter_client>=8",
  "bash_kernel",
  "plotnine>=0.15.5",
  "zensical>=0.0.43",
]

[tool.uv]
package = false


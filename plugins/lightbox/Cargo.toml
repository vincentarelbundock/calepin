[package]
name = "lightbox"
version = "0.0.1"
edition = "2021"
description = "calepin plugin: click-to-enlarge lightbox for HTML figures"

[lib]
crate-type = ["cdylib"]

[dependencies]
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }

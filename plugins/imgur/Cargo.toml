[package]
name = "imgur"
version = "0.0.1"
edition = "2021"
description = "calepin plugin: upload images to imgur via [path]{.imgur} spans"

[lib]
crate-type = ["cdylib"]

[dependencies]
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"

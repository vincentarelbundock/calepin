[package]
name = "txtfmt"
version = "0.0.1"
edition = "2021"
description = "calepin plugin: .txtfmt span with em, color, smallcaps, underline, mark"

[lib]
crate-type = ["cdylib"]

[dependencies]
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }

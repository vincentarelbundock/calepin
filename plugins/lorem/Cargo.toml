[package]
name = "lorem"
version = "0.0.1"
edition = "2021"
description = "calepin plugin: {{< lorem >}} shortcode for placeholder text"

[lib]
crate-type = ["cdylib"]

[dependencies]
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }

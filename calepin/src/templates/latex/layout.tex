{%- if clp.is_figure %}\begin{ {{-cfg.fig_env-}} }{{cfg.fig_pos}}
\centering
{% endif %}
{{clp.rows}}
{%- if clp.is_figure and cfg.caption %}
\caption{ {{-cfg.caption-}} }
{% endif %}
{%- if clp.is_figure and cfg.id %}\label{ {{-cfg.id-}} }
{% endif %}
{%- if clp.is_figure %}\end{ {{-cfg.fig_env-}} }{% endif %}

{%- if calepin.is_figure %}\begin{ {{-config.fig_env-}} }{{config.fig_pos}}
\centering
{% endif %}
{{calepin.rows}}
{%- if calepin.is_figure and config.caption %}
\caption{ {{-config.caption-}} }
{% endif %}
{%- if calepin.is_figure and config.id %}\label{ {{-config.id-}} }
{% endif %}
{%- if calepin.is_figure %}\end{ {{-config.fig_env-}} }{% endif %}

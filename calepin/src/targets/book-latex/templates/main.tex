\documentclass[11pt,openany]{book}
\usepackage[T1]{fontenc}
\usepackage{graphicx,hyperref}
\usepackage[margin=1in]{geometry}

{% include "preamble.tex" %}

{% if cfg.title %}\title{ {{cfg.title}} }{% endif %}
{% if cfg.authors %}\author{ {% for a in cfg.authors %}{{ a.name }}{% if not loop.last %} \and {% endif %}{% endfor %} }{% endif %}
\date{}

\begin{document}

\frontmatter
\maketitle
\tableofcontents

\mainmatter

{% for node in pages %}
{% if node.children %}
\part{ {{node.title}} }
{% for child in node.children %}
\chapter{ {{child.title}} }
\include{ {{child.file | replace(".tex", "")}} }
{% endfor %}
{% else %}
\chapter{ {{node.title}} }
\include{ {{node.file | replace(".tex", "")}} }
{% endif %}
{% endfor %}

\end{document}
